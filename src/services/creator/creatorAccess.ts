import { CreatorAccessService } from '@/services/creator/creatorAccessService';

/**
 * Convenience singleton for server-side creator access checks.
 */
export const creatorAccessService = new CreatorAccessService();
